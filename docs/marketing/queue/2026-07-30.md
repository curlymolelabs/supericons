# Queue: 2026-07-30 (first post, account debut)

Pillar: agent demo (debut post, becomes the pin). Owner posts everything; nothing here auto-publishes.

## Post 1: debut demo (recommended)

**Copy (main post):**

> Your AI agent just picked its own icons.
>
> Supericons MCP gives coding agents 20,000+ SVG icons across 11 libraries, searchable by meaning, with previews, in any of 11 languages.
>
> No API key. One config line.

**Shorter alt:**

> We gave AI agents an icon library they can actually use: search by meaning, preview visually, export SVG. 20,000+ icons, 11 libraries, no API key.

**Reply 1 (post immediately under it):**

> Try it in Claude Desktop, Cursor, or any MCP client:
>
> `npx -y @supericons/mcp`
>
> Then ask your agent: "Add appropriate icons to this dashboard using Supericons."
>
> Icons from Lucide, Tabler, Phosphor, Heroicons, Material Symbols, and more. https://supericons.dev

**Visual:** screen recording (30 to 45 seconds, or a 3-frame screenshot series if video is friction):
1. An agent chat where you type the money prompt below.
2. The agent calling search_icons or recommend_icons and showing the preview contact sheet.
3. The dashboard with icons applied.
Keep the whole terminal or editor window honest and uncluttered. Do not show any admin dashboards or telemetry data in frame (VC-3).

**Demo script (exact inputs):**

Setup off camera. Claude Code CLI: `claude mcp add supericons -- npx -y @supericons/mcp`. Other MCP clients: `"command": "npx", "args": ["-y", "@supericons/mcp"]`.

Stage prompt (off camera, run to completion first):

> Create a single-file dashboard.html for a fictional AI agent-ops tool called "Fleet". Dark theme, left sidebar with these nav items as text only, no icons yet: Dashboard, Agents, Workflows, Runs, Logs, Billing, Settings. Simple main area with three stat cards: Active Agents, Runs Today, Errors. Clean modern CSS, no external dependencies. Open it in the browser when done.

Money prompt (on camera):

> The sidebar has no icons. Use Supericons to find the right icon for each nav item, show me a visual preview of the candidates before choosing, then insert the winning SVGs inline next to each label and refresh the page.

Backup beats:
- Semantic flex: "Search Supericons for an icon meaning 'AI agent workflow' and tell me why you'd pick each result." (results carry use_when and avoid_when, so the reasoning reads well on camera)
- Visual compare: "Use Supericons to visually compare the top 5 icons for 'autonomous agent' and pick one for the Agents nav item."
- Multilingual: "Search Supericons for '工作流' and show me what comes back."

If the agent skips the preview, retake; the phrase "show me a visual preview of the candidates before choosing" forces the contact sheet. Keep chat and browser side by side; the sidebar gaining icons with no cut is the proof moment.

**Alt text for media:** "An AI coding agent searches Supericons over MCP, previews matching icons, and inserts them into a dashboard sidebar."

**Proof:**
- 11 libraries: live `list_libraries` probe this session returned exactly 11 (bootstrap, heroicons, iconoir, ionicons, lucide, material, mingcute, phosphor, si, simpleicons, tabler).
- 20,000+ icons: same probe, per-library counts sum to 21,467 icon entries.
- 11 languages: `search_icons` schema, supported locales zh-Hans, zh-Hant, ja, ko, es, de, pt, ar, hi, vi, th (11).
- Search works: live probe "AI agent workflow" returned 5 results with previews (lucide:bot, material:workflow, and others).
- No API key: keyless docs correction verified live on supericons.dev per project records; re-check the site copy once before posting.

REVIEW: PASS 2026-07-30 (verified inline this session; run x-content-reviewer on future queue files)

## Owner checklist

- [ ] Optional but recommended before the first post: set up the profile (bio, banner, link to supericons.dev, pinned later). Suggested bio: "Icons your AI agent can use. 20,000+ SVGs, 11 libraries, search by meaning over MCP. Free." No trailing period needed if tight on space.
- [ ] Record the demo per visual instructions.
- [ ] Post main copy with media, then Reply 1 under it.
- [ ] Pin the post.
- [ ] Append to docs/marketing/post-log.md:
  `| 2026-07-30 | Agent demo | debut | <link> | | | | pinned |`
