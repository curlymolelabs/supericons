# start server

npm run dev

npm run converter:proof-service

# quick commands

proceed to execute the plan accordingly. ensure you do not break existing codebase and dependencies.

audit and review the agent's work. read agent comments below. advise next step. If all good, provide prompt instruction with sufficient guidance and context.

audit your work, if all good, provide the instruction prompt to run the next batch with sufficient context and guidance.